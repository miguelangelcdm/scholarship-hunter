# Gemini API Key (Required for AI Extractor, Scoring, and Essays)
GEMINI_API_KEY="your_google_gemini_api_key"

# Maximum number of search results to fetch from DuckDuckGo per scan (Default: 5)
SEARCH_MAX_RESULTS=5

# Scrapy depth limit (Default: 1. Higher values scrape links inside the target pages)
SCRAPER_DEPTH_LIMIT=1

# Maximum characters to extract from a single webpage (Default: 5000). Helps prevent token burn.
SCRAPER_MAX_TEXT_LENGTH=5000

# Maximum total pages the scraper will crawl in a single execution to prevent runaway loops (Default: 8)
SCRAPER_MAX_PAGES=8

# --- Gatekeeper Logic ---
GATEKEEPER_MIN_WORDS=50
GATEKEEPER_MIN_DENSITY=0.2
MASS_DISCOVERY_SCAN_LIMIT=100

# --- LLM Scoring Business Rules ---
SCORE_CAP_MISSING_REQUIREMENTS=30
SCORE_CAP_MISSING_EXPERIENCE=20
SCORE_CAP_LANGUAGE_BARRIER=15
SCORE_TARGET_IMPROVEMENT=90
WEIGHT_TARGET_DISCIPLINES=40
WEIGHT_LOCATION=30
WEIGHT_CAREER_GOALS=30

# --- LLM Limits & Configuration ---
SCOUT_MAX_LINKS=200
MAX_PAGE_TEXT_LENGTH=25000
DEEP_SCAN_MAX_PAGE_LENGTH=40000
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4000
